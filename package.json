{
    "name": "douglasgreen/template",
    "description": "Project containing generic templates to use in starting other projects",
    "license": "MIT",
    "engines": {
        "node": ">=18",
        "npm": ">=9"
    },
    "devDependencies": {
        "@commitlint/cli": "^19.3",
        "@commitlint/config-conventional": "^19.2",
        "husky": "^9.0",
        "prettier": "^3.2"
    },
    "scripts": {
        "commitlint": "commitlint --edit",
        "lint:fix": "prettier --write .",
        "prepare": "husky"
    }
}
